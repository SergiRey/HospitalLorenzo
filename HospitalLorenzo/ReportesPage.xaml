<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HospitalLorenzo.ReportesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HospitalLorenzo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#f4f7fb" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Reporte Semanal"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#001e3b"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,8,0,16">
            <Button Content="Descargar PDF"
                    Background="#001e3b"
                    Foreground="White"
                    Click="BtnDescargarPdf_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="16" Margin="0,0,0,24">

            <Border Background="#001e3b" CornerRadius="10" Padding="20" Width="180">
                <StackPanel>
                    <TextBlock Text="Total Citas"
                               Foreground="#89c4ff" FontSize="13"/>
                    <TextBlock x:Name="TxtTotalCitas"
                               Text="0"
                               Foreground="White"
                               FontSize="36"
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Background="#0a4a2e" CornerRadius="10" Padding="20" Width="180">
                <StackPanel>
                    <TextBlock Text="Total Pacientes"
                               Foreground="#89ffb5" FontSize="13"/>
                    <TextBlock x:Name="TxtTotalPacientes"
                               Text="0"
                               Foreground="White"
                               FontSize="36"
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Background="#4a2e0a" CornerRadius="10" Padding="20" Width="180">
                <StackPanel>
                    <TextBlock Text="Total Doctores"
                               Foreground="#ffd589" FontSize="13"/>
                    <TextBlock x:Name="TxtTotalDoctores"
                               Text="0"
                               Foreground="White"
                               FontSize="36"
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>

        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Citas de la semana"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="#001e3b"
                       Margin="0,0,0,12"/>

            <ListView Grid.Row="1" x:Name="ListaCitasSemana">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Cita">
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="ID" Foreground="#888" FontSize="11"/>
                                    <TextBlock Text="{x:Bind Id}" FontWeight="Bold" Foreground="#001e3b"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Paciente" Foreground="#888" FontSize="11"/>
                                    <TextBlock Text="{x:Bind PacienteNombre}" FontWeight="SemiBold" Foreground="#001e3b"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Doctor" Foreground="#888" FontSize="11"/>
                                    <TextBlock Text="{x:Bind DoctorNombre}" Foreground="#001e3b"/>
                                </StackPanel>

                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Fecha y hora" Foreground="#888" FontSize="11"/>
                                    <TextBlock Foreground="#001e3b">
                                <Run Text="{x:Bind Fecha}"/>
                                <Run Text=" - "/>
                                <Run Text="{x:Bind Hora}"/>
                                    </TextBlock>
                                </StackPanel>

                                <Border Grid.Column="4" CornerRadius="6" Padding="8,4" VerticalAlignment="Center" Background="#e8f4ff">
                                    <TextBlock Text="{x:Bind Estado}" Foreground="#4879AB" FontSize="12"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>